[
  {
    "name": "greeter-log",
    "command": "f=$(ls -t \"${CLAUDE_PLUGIN_ROOT:-.}/skills/hello/logs\"/*.log 2>/dev/null | head -1); tail -n0 -F \"$f\" | grep --line-buffered -E '^\\[(done|error)\\]' | awk '$0!=p{print;fflush();p=$0}'",
    "description": "greeter skill log — fires when a greeting completes ([done]) or errors ([error])"
  }
]
