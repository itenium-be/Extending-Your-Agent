[
  {
    "name": "greeter-log",
    "command": "D=\"${CLAUDE_PLUGIN_ROOT}/skills/hello/logs\"; mkdir -p \"$D\"; f=\"$D/${CLAUDE_CODE_SESSION_ID}.log\"; touch \"$f\"; tail -F -n0 \"$f\" 2>/dev/null | grep --line-buffered -E '^\\[(done|error)\\]'",
    "description": "greeter skill log — fires when a greeting completes ([done]) or errors ([error])"
  }
]
